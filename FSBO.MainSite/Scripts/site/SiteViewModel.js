/// <reference path="../_reference.js" />

function SiteViewModel() {
	var self = this;

	//self.menuitems = ko.observableArray(menuModel.items);

	//self.menuClick = function (data, event) {
	//	console.log('clicked menu option', data);

		/*$(pageContainerId).load(data.viewRoute, function () {
			ko.cleanNode(pageContainer);
			ko.applyBindings(data.viewModel, pageContainer);
		});*/

		/*if (data.name == "About Page") {
			$(pageContainerId).load("/About", function () {
				ko.cleanNode(pageContainer);
				ko.applyBindings(countryViewModel, pageContainer);
			});

		} else if (data.name == "Home Page") {
			$(pageContainerId).load("/Home", function () {
				ko.cleanNode(pageContainer);
				ko.applyBindings(countryViewModel, pageContainer);
			});
		}*/
	//};
}